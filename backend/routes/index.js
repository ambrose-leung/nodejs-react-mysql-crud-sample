const healthRoutes = require('./health');
const userLookupRoutes = require('./userLookup');
const addUserRoutes = require('./addUser');
const deleteUserRoutes = require('./deleteUser');

module.exports = {
  healthRoutes,
  userLookupRoutes,
  addUserRoutes,
  deleteUserRoutes
};
